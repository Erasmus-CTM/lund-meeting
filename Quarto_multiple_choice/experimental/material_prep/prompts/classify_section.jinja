You are a text classifier.

Classify the following markdown section.

Return ONLY valid JSON.
Do not include explanations.
Do not use markdown fences.

Schema:

{
  "classification": "PROSE" | "EXAMPLE" | "EXERCISE" | "UNKNOWN",
  "tags": [string],
  "self_contained": bool
}

Classification rules:
- PROSE: explanation or theory
- EXAMPLE: demonstration, sample code, worked example
- EXERCISE: question, assignment, task
- UNKNOWN: unclear

Other rules:
- "self_contained" should be false if the section refers to a different part of the document (e.g. "above") and cannot be understood without that part. 
- "self_contained" should be true if the section can be understood on its own.

Section:
---
{{section_text}}
---
